export const fonts = {
  heading: "'Nunito Sans', system-ui, -apple-system, sans-serif",
  body: "'Nunito Sans', system-ui, -apple-system, sans-serif",
};

export const fontSizes = {
  "display-xl": "clamp(3rem, 7vw, 5.5rem)",
  "display-lg": "clamp(2.25rem, 5vw, 4rem)",
  "display-md": "clamp(1.75rem, 3.6vw, 2.75rem)",
  "display-sm": "clamp(1.4rem, 2.6vw, 2rem)",
};

export const letterSpacings = {
  tightest: "-0.04em",
  tighter: "-0.025em",
  tight: "-0.015em",
};
