/**
 * Placeholder settings repository. Profile edits go through the auth
 * repository (`updateProfile` / `uploadAvatar`); add product-specific
 * settings endpoints here as the app grows.
 */
export interface SettingsRepository {}
